
import PropTypes from 'prop-types';

Header.propTypes = {
    
};

function Header() {
    return (
        <div>
            <h1>Course Registration</h1>
        </div>
    );
}

export default Header;
